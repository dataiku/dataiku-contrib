{
    "meta" : {
        "label" : "To Tableau extract",
        "description": "This exporter produces TDE (Tableau extract) files which you can then download to your local machine. Beware, exporting a large dataset to a TDE file may take long. If your dataset contains several millions records, you may want for instance to have DSS write into a SQL database usable by Tableau."
    },

    "exportBehavior" : "OUTPUT_TO_FILE",

    "mime" : {
        "mimeType" : "application/tde",
        "extension" : ".tde"
    },

    "exportOptions": [
        {
            "label" : "Tableau extract (.tde)",
            "compression" : "NONE"
        }
    ],

    "params": [
    ]
}
