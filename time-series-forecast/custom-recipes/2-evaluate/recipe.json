// This file is the descriptor for the Custom code recipe clean
{
    // Meta data for display purposes
    "meta" : {
        "label" : "2. Forecast - evaluate",
        "description" : "Evaluate the forecasting performance of previously trained models" ,
        "icon" : "icon-star"
    },

    "kind" : "R",
    
    "selectableFromDataset" : "input_folder",
    

    // Inputs and outputs are defined by roles. In the recipe's I/O tab, the user can associate one
    // or more dataset to each input and output role.

    // The "arity" field indicates whether the user can associate several datasets to the role ('NARY')
    // or at most one ('UNARY'). The "required" field indicates whether the user is allowed to
    // associate no dataset with the role.

    "inputRoles" : [
        {
            "name": "input_folder",
            "label": "Saved model folder",
            "description": "Contains saved models from the \"Train\" recipe",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": false,
            "acceptsManagedFolder": true,
            "mustBeStrictlyType": "Filesystem"
        }
    ],

    "outputRoles" : [
        {
            "name": "output_dataset",
            "label": "Evaluation dataset",
            "description": "Table of evaluation results",
            "arity": "UNARY",
            "required": false,
            "acceptsDataset": true,
            "acceptsFolder": false
        } 
        // ...
    ],

    // The field "params" holds a list of all the params
    // for wich the user will be prompted for values in the Settings tab of the recipe.

    // The available parameter types are:
    // STRING, INT, DOUBLE, BOOLEAN, PASSWORD, SELECT, MAP, COLUMN, COLUMNS, TEXTAREA

    "params": [
		
        {
            "name": "TRAIN_TEST_SPLIT_ACTIVATED",
            "label" : "Train/test split strategy",
            "description": "Apply simple train/test temporal split strategy",
            "type": "BOOLEAN",
            "defaultValue" : true,
            "mandatory" : false
        },
        
        {
            "name": "TEST_SIZE",
            "label" : "\u21B3 Test periods",
            "description": "Number of periods to use as test from the original dataset",
            "type": "INT",
            "defaultValue" : 10,
            "mandatory" : false,
            "visibilityCondition": "model.TRAIN_TEST_SPLIT_ACTIVATED"
        },
        
        {
            "name": "TSCROSSVALIDATION_ACTIVATED",
            "label" : "Times series CV strategy",
            "description": "Cross-validation strategy for time series cf. https://robjhyndman.com/hyndsight/tscv/",
            "type": "BOOLEAN",
            "defaultValue" : true,
            "mandatory" : false
        },
        
        {
            "name": "CROSSVALIDATION_HORIZON",
            "label" : "\u21B3 Prediction horizon",
            "description": "Number of periods to predict in advance",
            "type": "INT",
            "defaultValue" : 1,
            "mandatory" : false,
            "visibilityCondition": "model.TSCROSSVALIDATION_ACTIVATED"
        }

    ],

    // The field "resourceKeys" holds a list of keys that allows to limit the number
    // of concurrent executions and activities triggered by this recipe.
    //
    // Administrators can configure the limit per resource key in the Administration > Settings > Flow build
    // screen.

    "resourceKeys" : []

}
