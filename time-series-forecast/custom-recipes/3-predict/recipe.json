// This file is the descriptor for the Custom code recipe clean
{
    // Meta data for display purposes
    "meta" : {
        "label" : "3. Forecast - predict",
        "description" : "Use trained models to predict future values with confidence intervals" ,
        "icon" : "icon-signal"
    },

    "kind" : "R",
    
    "selectableFromDataset" : "input_dataset",

    // Inputs and outputs are defined by roles. In the recipe's I/O tab, the user can associate one
    // or more dataset to each input and output role.

    // The "arity" field indicates whether the user can associate several datasets to the role ('NARY')
    // or at most one ('UNARY'). The "required" field indicates whether the user is allowed to
    // associate no dataset with the role.

    "inputRoles" : [
        {
            "name": "input_dataset",
            "label": "Input dataset",
            "description": "Must include one date and one numerical column",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true,
            "acceptsFolder": false
        }
    ],

    "outputRoles" : [
        {
            "name": "output_dataset",
            "label": "Output dataset",
            "description": "Will output cleaned date and numerical columns",
            "arity": "UNARY",
            "required": true,
            "acceptsDataset": true,
            "acceptsFolder": false
        } 
        // ...
    ],

    // The field "params" holds a list of all the params
    // for wich the user will be prompted for values in the Settings tab of the recipe.

    // The available parameter types are:
    // STRING, INT, DOUBLE, BOOLEAN, PASSWORD, SELECT, MAP, COLUMN, COLUMNS, TEXTAREA

    "params": [
		
		{
            "name": "TIME_COLUMN",
			"label" : "Time column",
			"description":"Must be a parsed date",
            "type": "COLUMN",
            "columnRole":"input_dataset",
			"mandatory" : true
        },
		
		{
            "name": "SERIES_COLUMN",
			"label" : "Series column",
			"description":"Must be numeric",
            "type": "COLUMN",
            "columnRole":"input_dataset",
			"mandatory" : true
        },
		
		// A "SELECT" parameter is a multi-choice selector. Choices are specified using the selectChoice field
        {
            "name": "CHOSEN_GRANULARITY",
            "type": "SELECT",
			"mandatory" : true,
            "selectChoices": [
                {
                    "value": "year",
                    "label": "Year"
                },
				{
                    "value": "quarter",
                    "label": "Quarter"
                },
				{
                    "value": "month",
                    "label": "Month"
                },
                {
                    "value": "week",
                    "label": "Week"
                },
				{
                    "value": "day",
                    "label": "Day"
                },
				{
                    "value": "hour",
                    "label": "Hour"
                },
            ]
        },
		
        {
            "name": "TIMEZONE",
            "label" : "Time zone",
            "type": "STRING",
            "description":"Follow ISO 8601 standard (default is UTC)",
            "defaultValue" : "UTC",
			"mandatory" : false
        },
		
		{
            "name": "REPLACE_MISSING",
			"label" : "Replace missing values",
			"description":"Interpolate missing values",
            "type": "BOOLEAN",
            "defaultValue" : true,
			"mandatory" : false
        },
		
        {
            "name": "REPLACE_OUTLIER",
			"label" : "Replace outliers",
			"description":"Identify and replace outliers (optional)",
            "type": "BOOLEAN",
            "defaultValue" : false,
			"mandatory" : false
        }


    ],

    // The field "resourceKeys" holds a list of keys that allows to limit the number
    // of concurrent executions and activities triggered by this recipe.
    //
    // Administrators can configure the limit per resource key in the Administration > Settings > Flow build
    // screen.

    "resourceKeys" : []

}
